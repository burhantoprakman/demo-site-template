/**
 * TODO(fork): the only file most people need to edit before publishing.
 */
export const site = {
  /** Your demo's name, shown in the header. */
  name: 'Demo Site Template',
  /** One line under the name. Keep it to what this site explains. */
  tagline: 'A starting point for internal explainer and demo sites',
  /** Who to contact about this site. Required by the content rules. */
  owner: { name: 'Your Name', email: 'you@company.com' },
  /** Top navigation. Keep to four items or fewer. */
  nav: [
    { label: 'Overview', href: '/' },
    { label: 'Example page', href: '/example/' },
    { label: 'Components', href: '/kitchen-sink/' },
  ],
};
