# Copy to .env and fill in. Never commit .env.
# Nothing here is required for local development.

# Optional analytics ID for internal hosting
PUBLIC_ANALYTICS_ID=

# Optional banner shown on every page, e.g. "Internal demo - not a product"
PUBLIC_DEMO_BANNER=
